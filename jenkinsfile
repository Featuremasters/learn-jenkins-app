pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello World'
            }
        }
        stage('test') {
            steps {
                sh '''
                    ls -la
                '''
            }
        }
    }
}
